# CPU Grid – Auth + Admin Console

This repo contains:
- `auth-service/` (Spring Boot Auth Service)
- `ui-next/` (Next.js Admin/User UI)

## Requirements
- Java 17 (recommended)
- Node.js 18+ (or 20)
- MySQL (XAMPP or MySQL Server)

## 1) Database setup
Create an empty database:
```sql
CREATE DATABASE cpu_grid_auth;
